$(window).on('keydown', (event) => {
    if (event.keyCode === 27) { // Clicking on Escape
        
    }
});

window.addEventListener('message', (event) => {
    const data = event.data;

    if (data.type === 'example') {
        
    }
})